import styled from "styled-components";
export const List = styled.ul`
  list-style: none;
  display: grid;
  padding: 1.3em;
  border-radius: 5px;
  margin: 1.5em;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.26);
`;
